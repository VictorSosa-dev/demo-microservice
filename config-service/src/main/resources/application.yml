server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/VictorSosa-dev/demo-microservice
          default-label: 08-circuit-breaker
          skipSslValidation: true
          search-paths: config-data
  application:
    name: config-service