server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/VictorSosa-dev/demo-microservice
          default-label: 05-hystrix
          skipSslValidation: true
          search-paths: config-data
  application:
    name: config-service