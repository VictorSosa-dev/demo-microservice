server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: 02-Config
          uri: https://github.com/VictorSosa-dev/demo-microservice
          search-paths: config-data
  application:
    name: config-service