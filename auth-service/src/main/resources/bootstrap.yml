spring:
  application:
    name: auth-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  boot:
    admin:
      client:
        url: http://localhost:8086
jwt:
    secret: gMQf10usnA